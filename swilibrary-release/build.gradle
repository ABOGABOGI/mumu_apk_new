configurations.maybeCreate("default")
artifacts.add("default", file('swilibrary-release.aar'))

gradle.projectsEvaluated {
    tasks.withType(JavaCompile) {
        options.compilerArgs << "-Xlint:deprecation"
    }
}